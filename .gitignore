/flow
/flowde
/bin/flow
*.db

# flow's own data tree accidentally created in the repo root when
# running the CLI with a relative FLOW_ROOT.
/projects/
/tasks/

# git worktrees for isolated feature work
/.worktrees/
/.playwright-mcp

# flow-managed agent worktrees
/.codex/worktrees/

# flow-managed agent worktrees
/.claude/worktrees/

# Superpowers brainstorming companion (local mockups)
.superpowers/

# UI build toolchain deps (built assets under static/ stay committed)
/internal/server/ui/node_modules/
# wterm.wasm is copied from node_modules into public/ at build time, then
# emitted to static/ (which is committed). The public/ copy is regenerated.
/internal/server/ui/public/wterm.wasm
